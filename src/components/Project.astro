---
import type { HTMLAttributes } from "astro/types";

import Card from "./Card.astro";
import { Button } from "./Button";

export interface Props extends HTMLAttributes<"div"> {
  title: string;
  web: string;
  framework: string;
  ui: string;
  components: string;
  database: string;
  button: string;
  url: string;
  variant: string;
}

const { title, web, framework, ui, components, database, button, url, variant, ...attrs } =
  Astro.props as Props;
---

<div {...attrs}>
  <Card>
    <h2 class="relative z-10 text-lg font-bold">{title}</h2>
    <ul class="relative z-10 flex flex-row flex-wrap gap-2 mb-4">
      <li class="text-sm text-default-900/50 lowercase">{web}</li>
      <li class="text-sm text-default-900/50 lowercase">{framework}</li>
      <li class="text-sm text-default-900/50 lowercase">{ui}</li>
      <li class="text-sm text-default-900/50 lowercase">{components}</li>
      <li class="text-sm text-default-900/50 lowercase">{database}</li>
    </ul>
    <slot />
    <a class="w-fit h-fit" href={url}>
      <Button className="relative z-10">{button}</Button>    
    </a>
  </Card>
</div>
